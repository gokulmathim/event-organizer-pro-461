asgiref==3.8.1
Django==5.2
djangorestframework==3.16.0
djangorestframework-simplejwt==5.3.1  # Useful if JWT support is needed
drf-yasg==1.21.10
flake8==7.2.0
inflection==0.5.1
iniconfig==2.1.0
mccabe==0.7.0
packaging==24.2
pluggy==1.5.0
pycodestyle==2.13.0
pyflakes==3.3.2
pytz==2025.2
PyYAML==6.0.2
sqlparse==0.5.3
uritemplate==4.1.1
django-cors-headers==4.7.0

# For documentation builds and linting in CI
tox==4.11.4  # Optional: for running tests and linting in environments
coverage==7.5.1  # Optional: for code coverage reporting

# Note:
# If you have environment-specific tools, install them via:
# pip install -r requirements.txt
# Then pin your deployed environment fully with `pip freeze > requirements.txt`
